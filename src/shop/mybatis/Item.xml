<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper PUBLIC "-//ibatis.apache.org//DTD Mapper 3.0//EN" 

"http://ibatis.apache.org/dtd/ibatis-3-mapper.dtd">

<mapper namespace="item">

	<select id="list" resultType="Item">
		select * from item order by idx
	</select>
	<insert id="insert">
		
	</insert>

	<select id="detail" resultType="Item">
		select * from item where idx = #{idx}
	</select>
	
	<update id="update" parameterType="Item">
	update item set itemName=#{itemName} , itemPrice= #{itemPrice} , itemContent= #{itemContent} 
	, itemCount= #{itemCount} , url= #{url} where itemid=#{itemId} 
	</update>
	<update id="updateCount" parameterType="Item">
	update item set  itemCount = # {itemCount} ,  where itemid= # {itemId} 
	</update>
	
	<delete id="delete">
	delete from item where itemId = #{itemId}
	
	</delete>

</mapper>